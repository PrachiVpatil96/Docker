# Use official Nginx image as the base
FROM nginx:latest

# Copy the custom HTML file to the Nginx server's root directory
COPY index.html /usr/share/nginx/html/

# Expose port 80 (default Nginx port)
EXPOSE 80
